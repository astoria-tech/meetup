---
import Layout from './Layout.astro'

type Props = {
  title?: string
  bodyClass?: string[]
}
const {title, bodyClass = []} = Astro.props
---

<Layout useWavyBackground title={title} bodyClass={['bg-zinc-100', ...bodyClass]}>
  <div class="lg:w-[500px] m-auto">
    <slot />
  </div>
</Layout>
